program Logicals is

global variable x : bool;
global variable c : string;
global variable tmp : bool;
global variable bs : string;

begin

x := getBool();
if(bs & x) then
	tmp := putString("T");
else
	tmp := putString("F");
end if;

c := getString();
if(c == "A") then
	tmp := putString("T");
else
	tmp := putString("F");
end if;

end program.
